# === Manual Test Case ===

# 1. Submit the program
submit lifecycle_test.lang

# 2. Run in background (Append '&' so shell remains active)
run 1 &

# 3. List jobs to find the PID
jobs

# 4. Kill the process (Use the PID from 'jobs' output)
# Example: kill 12345
kill <PID>

# 5. Memstat (Checking Shell Command)
memstat

# 6. Debug (Interactive VM Debugger)
debug 1
# Inside debug mode, you can type 'memstat' to see actual memory usage.
